# CAD

`generate_v2.py` contains the parametric core geometry for the V2 reconstruction.

All release STEP/STL assets, including auxiliary holders and experiment jigs, are committed so users do not need the CAD toolchain just to print or inspect the model.

Photogrammetric working dimensions are documented in `docs/research/photogrammetry.md`.
