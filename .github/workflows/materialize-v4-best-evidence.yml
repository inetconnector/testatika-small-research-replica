name: Legacy M2 V4 visual-reference generator (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'The former all-solid M2 V4 output is reference-only.'
          echo 'Primary physical build output is hardware/build-kits/m2-v5.'
          echo 'Legacy source is preserved under cad/reference/.'
