name: Legacy M6 V1 visual-reference generator (manual only)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'The former all-solid M6 V1 output is reference-only.'
          echo 'Primary physical build output is hardware/build-kits/m6-v2.'
          echo 'Legacy source is preserved under cad/reference/.'
